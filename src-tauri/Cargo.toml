[package]
name = "island-app"
description = "Top Island Tauri application shell"
version.workspace = true
edition.workspace = true

[build-dependencies]
tauri-build = { version = "2", features = [] }

[lib]
name = "island_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
island-core = { path = "../crates/island-core" }
island-windows = { path = "../crates/island-windows" }
island-wechat = { path = "../crates/island-wechat" }
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-single-instance = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
ureq = { version = "3", features = ["json"] }
tungstenite = "0.30"
winreg = "0.52"
# 解酷狗本地 .krc 歌词（XOR + zlib）；crate 已在依赖树里（ureq/png 传递依赖），不新增第三方代码
flate2 = "1"
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_Globalization",
    "Win32_System_Diagnostics_Debug",
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_Kernel",
    "Win32_System_Registry",
    "Win32_System_SystemInformation",
    "Win32_System_Threading",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
tauri-plugin-updater = "2.11.0"

[lints]
workspace = true
